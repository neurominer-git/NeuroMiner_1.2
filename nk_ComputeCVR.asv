function [CVR, SE] = nk_ComputeCVR(SUM, SEL)

SUM2 = nm_nansum(SUM,2).^2;
SUM =
SQ = sqrt(SUM2 ./ SEL - (SUM ./ SEL).^ 2);
SE = SQ./sqrt(SEL)*1.96;

% Mean relevance/weight vector across partitions
MN = nm_nansum(SUM, 2) ./ SEL;

% Compute CVR
CVR = MN./SE;